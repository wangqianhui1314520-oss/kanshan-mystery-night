# ============ 凭证红线（官方要求：App Key / Access Secret / OAuth Token 不得进仓库） ============
.env
**/.env
.env.*
!.env.example
*.secret

# ============ 运行数据 / 缓存 / 日志 ============
game/data/
game/agents/cache/
game/.pytest_cache/
frontend/.pytest_cache/
**/__pycache__/
*.pyc
*.log

# ============ 大文件（视频走外链，不进仓库） ============
*.mp4

# ============ 本地开发临时产物（压测探针 / Playwright 输出，不属于交付资产） ============
game/.test-tmp-run2/
game/tmp_pytest_run/
game/_party_stress/
game/frontend/_*.txt

# ============ WorkBuddy 会话与临时目录 ============
2026-*/
pytest-cache-files-*/
zhihu-cli-skill.zip
.wbapp_*.genie

# WorkBuddy 内部目录（记忆/密钥/缓存，绝不进仓库）
.workbuddy/
.playwright-cli/
